#include "header.h"

void dInterpolative_QR(double* M, int m, int n, int maxdim, double* C, double* Z)
{
    int k = std::min(m, n);
    maxdim = maxdim < k ? maxdim : k;
    k = maxdim;
    
    return;        
}

void TT_ID()
{

}